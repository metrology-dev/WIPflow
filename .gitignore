# Export files — generated data, not source
*.labwip
*.csv
helger_2026.csv

# Scratch / temp
notes_temp.txt
*.tmp
*.bak

# Icons source folder — logo embedded as base64 in WIPflow.html
Icons/

# OS
Thumbs.db
.DS_Store
desktop.ini

# Editor / IDE
.vscode/
*.swp
*~

# Claude Code local settings (contains machine-specific paths)
.claude/

# Node.js / testing
node_modules/
package-lock.json

# Playwright artifacts
test-results/
playwright-report/
blob-report/
.playwright/

# Test data — generated by fixtures/generate-golden.mjs; fixture .wipflow files are checked in
# tasks.json and tasks.backup.json are user data files, never part of source
tasks.json
tasks.backup.json
